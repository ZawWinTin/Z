import { createButton, getDialogPos } from '@/Composables/NoteEditor/UI/Util';

export const addTableHandles = (editor, editorContainer, dialog) => {
    let element = null;

    editor.tableHandles.onUpdate(imageToolbarState => {});
};
