import { createButton, getDialogPos } from '@/Composables/NoteEditor/UI/Util';

export const addImageToolbar = (editor, editorContainer, dialog) => {
    let element = null;

    editor.imageToolbar.onUpdate(imageToolbarState => {});
};
